name: mobile_jkn_sentiment_ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_dir: {type: string, default: "mobile_jkn_reviews_preprocessing"}
      random_state: {type: int, default: 42}
    command: "python modelling.py --data-dir {data_dir} --random-state {random_state}"
